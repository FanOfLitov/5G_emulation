# Experiment 03 — Dynamic load

Applies reproducible `tc netem` network conditions and measures the RTT response over time.

> This first version emulates changing network conditions. It does not yet let the RL policy apply `tc` actions online.

```bash
sudo ./experiments/exp03_dynamic_load/run.sh
```
