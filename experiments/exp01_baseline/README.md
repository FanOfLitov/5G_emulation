# Experiment 01 — Baseline

Measures zero-touch startup time, UE address allocation, RTT and packet loss without changing the learned policy.

```bash
./experiments/exp01_baseline/run.sh
```

Set repetitions and ping target in `config.env`.
