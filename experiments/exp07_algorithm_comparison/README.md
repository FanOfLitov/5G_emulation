# Experiment 07 — Algorithm comparison

This directory reserves the comparison between Q-learning, EXP3 and DQN.

At present only Q-learning exists in the project. The script deliberately records EXP3 and DQN as `planned` instead of fabricating results.

```bash
./experiments/exp07_algorithm_comparison/run.sh
```
